"use client";

const HeartbeatCursor = () => {
  // We are literally just dipping out immediately 🏃‍♂️💨
  // Returning null means React renders absolutely nothing.
  // Your imports stay happy, your app compiles, and your default cursor gets to cook.
  return null;
};

export default HeartbeatCursor;